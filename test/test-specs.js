(function () {
  'use strict';
  window.testSpecs = [
    () => {
      describe('alt', () => {
        before(() => {

        });

        it('adds an error for images without an alt tag');
        it('does not add an error for images with an alt tag');
      });
    },
  ];
}());