{
  "name": "accessibility-linter",
  "version": "1.6.0",
  "description": "Accessibility test a web page or web application",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint . --ext .js",
    "mkbuild": "mkdir -p build",
    "clean": "rm -f build/* build/.gitignore",
    "build:standalone": "node lib/build/standalone | derequire | exorcist build/linter.js.map > build/linter.js",
    "uglify:standalone": "uglifyjs build/linter.js > build/linter.min.js",
    "build:umd": "node lib/build/umd | exorcist build/umd.js.map > build/umd.js",
    "uglify:umd": "uglifyjs build/umd.js > build/umd.min.js",
    "build:specs": "node lib/build/specs > test/specs/_rules.js",
    "build:dev": "npm run mkbuild && npm run build:umd && npm run build:specs",
    "build:all": "npm run build:standalone && npm run build:umd && npm run build:specs",
    "gh-pages": "git checkout gh-pages && git merge master -X theirs && npm run build && echo \"!**\" > build/.gitignore && git add -A && git commit -m \"merge master and latest build\" && git push && git checkout master",
    "prebuild": "npm run mkbuild && npm run clean && npm run lint",
    "build": "npm run build:all && npm run uglify:standalone && npm run uglify:umd",
    "watch": "watch 'npm run build:dev' lib"
  },
  "author": "Daniel Lewis",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.1.0",
    "escape-html": "^1.0.3",
    "eslint": "^3.9.1",
    "eslint-config-airbnb-base": "^10.0.1",
    "eslint-plugin-import": "^2.2.0",
    "exorcist": "^0.4.0",
    "expect": "^1.20.2",
    "highlight.js": "^9.7.0",
    "jquery": "^3.1.1",
    "marked": "^0.3.6",
    "mocha": "^3.0.2",
    "strictify": "^0.2.0",
    "uglify-js": "https://github.com/mishoo/UglifyJS2#harmony",
    "watch": "^1.0.1"
  },
  "dependencies": {
    "glob": "^7.1.1"
  }
}
